#include "list.h"

int main()
{
	t_list *my;

	
	// my = shell_split("hi    <<     name");
	// show_list(my);
	// my = shell_split("hi    <<     name");
	// show_list(my);
	// my = shell_split("hi    <<     name");
	// show_list(my);
	// my = shell_split("hi    <<     name");
	// show_list(my);
	// my = shell_split("hi    <<     name");
	// show_list(my);
	// my = shell_split("hi    <<     name");
	// show_list(my);
	my = shell_split("hi");
	show_list(my);



	return 0;
}